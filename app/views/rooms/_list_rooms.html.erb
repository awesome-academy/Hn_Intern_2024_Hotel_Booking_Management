<div class="row g-4" id="show-list-rooms">
  <% if @rooms.count > 0 %>
    <%= render @rooms %>
    <%== pagy_bootstrap_nav(@pagy) %>
  <% else %>
    <h3 class="text-center text-secondary">
      <%= t ".not_found" %>
    </h3>
  <% end %>
</div>
